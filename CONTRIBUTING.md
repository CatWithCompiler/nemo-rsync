- Use install-dev.sh to create symlinks to your Nemo-python extensions folder while modifying things.

Note:
## Running from source

Run the backend from the project root:

```bash
python3 -m backend.nemo_rsync <sources...>
```

The backend is part of the `backend` package and should be launched as a module.